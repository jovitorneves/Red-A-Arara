using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EspinhoMorte : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //red a arara
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
